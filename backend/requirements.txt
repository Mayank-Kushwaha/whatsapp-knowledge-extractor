# Web framework
fastapi==0.115.12
uvicorn[standard]==0.34.3
python-multipart==0.0.20

# Database
sqlalchemy==2.0.41
alembic==1.15.2
# Turso (libSQL) dialect for SQLAlchemy — used when TURSO_DATABASE_URL is set.
# libSQL is a SQLite fork; the same schema (incl. FTS5) and ORM queries work
# unchanged. Without these env vars the engine falls back to a local SQLite
# file so dev still works offline.
sqlalchemy-libsql==0.2.0

# Media storage — Cloudinary (used when MEDIA_BACKEND=cloudinary).
# Free tier is 25 GB storage + 25 GB monthly bandwidth.
cloudinary==1.41.0

# HTTP client
httpx==0.28.1

# HTML parsing
beautifulsoup4==4.13.4

# NLP / ML
scikit-learn==1.6.1
hdbscan==0.8.40
numpy==2.2.6
# Optional local-CPU embedding fallback. Only needed if EMBEDDING_PROVIDER=local
# (e.g. for development without hitting Gemini's free-tier daily quota of
# 1000 embed requests). Pulls in PyTorch (~500MB) on install. Render free
# tier (512MB RAM) will OOM with this loaded — keep EMBEDDING_PROVIDER=gemini
# on Render unless you upgrade to a tier with more memory.
sentence-transformers==4.1.0

# PDF extraction
pymupdf==1.25.5

# LLM providers
google-generativeai==0.8.5

# Auth — Google ID token verification
google-auth==2.35.0

# Environment
python-dotenv==1.1.0

# Testing
pytest==8.3.5
pytest-asyncio==0.26.0
